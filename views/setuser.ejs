<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body{
            background-color:#e4e4e4;
        }
        i{
            cursor: pointer;
        }
        .edit_profile{
            position: relative;
            left:50%;
            top:50%; 
            transform:translateX(-50%);
            width:300px;
            height: auto;
        }
        form{
            border: 1px solid rgba(0,0,0,0.2);
            border-radius: 7px;
            width:300px;
            height:auto;
            display: flex;
            flex-direction: column;
            background-color:#fff;
            padding:30px 20px;
        }
        .setImg{
            position:relative;
            left:50%;
            transform: translateX(-50%);
            width:100px;
            height: 100px;
        }
        .setImg img{
            border:6px solid rgba(9, 245, 9, 0.5);
            border-radius: 50%;
        }
        .upImg{
            position: absolute;
            width:20px;
            height:20px;
            right:0;
            bottom: 0;
        }
        .upImg input{
            position: absolute;
          /*  display: block;
            opacity: 0;*/
            width: 40px;
            right:0px;
            bottom:10px ;
            z-index: 10;
            opacity: 0;
        }
        .upImg i{
            font-size: 20px;
            position: absolute;
            bottom: 10px;
            right:0px;
            padding: 5px;
            background-color:rgba(9, 245, 9, 0.5);
            border-radius: 50%;
            border: 2px solid rgba(9, 245, 9, 0.5);
        }
        input[type = 'text']{
            position: relative;
            margin-top: 30px;
            width:150px;
            left:50%;
            transform: translateX(-50%);
            border:2px solid rgba(9, 245, 9, 0.5);
            border-radius:24px;
            padding:10px 20px;
            transition:0.25s;
        }
        input[type = 'text']:focus{
            outline: none;
            border: 2px solid orange;
        }
        .btn-send{
            width:30px;
            height:30px;
            position: relative;
            left: 50%;
            transform: translateX(-50%);
            margin-top:20px;
        }
        .btn-send button{
            opacity: 0;
            z-index: 10;
            padding: 10px;
            position: absolute;
        }
        .btn-send i{
            position: absolute;
            font-size: 30px;
            color: rgba(9, 245, 9, 0.5);
        }
        .back{
            position: absolute;
            font-size: 80px;
            color:aqua;
        }
    </style>
</head>
<body>
    <a href="/message" class="back"><i class="fa fa-arrow-circle-left"></i></a>
    <div class="edit_profile">
        <h1 style="color:rgba(0,0,0,0.4); text-align: center; margin-left: 40px;">edit client profile</h1>
        <form  action="edit_user" method="post" enctype="multipart/form-data">
            <span class="setImg">
                <img src="img/annoprofile.jpg" width="100" height="100" alt="">
                <div class="upImg">
                    <input type="file" name="img">
                    <i class="fa fa-wrench"></i>
                </div>
            </span>
            <input type="text" name="name">
            <div class="btn-send">
                <button></button>
                <i class="fa fa-wrench"></i>
            </div>
        </form>
    </div>
</body>
</html>