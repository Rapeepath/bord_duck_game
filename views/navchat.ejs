<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        *{
            margin:0;
            padding:0;
            box-sizing: border-box;
        }
        body{
            background-color:#e4e4e4;
        }
        nav{
            display: none;
            width:200px;
            position:absolute;
            bottom: 0;
            top:50px;
            background-color:#fff;
            border-radius:10px;
            box-shadow:5px 7px 10px rgba(0,0,0,0.5);
        }
        nav ul{
            position:relative;
            list-style-type:none;
        }
        nav ul li a{
            display:flex;
            align-items: center;
            font-size:1em;
            color:rgba(0,0,0,0.5);
            text-decoration:none;
            text-transform: capitalize;
            padding:10px 30px;
            height:60px;
            transition:0.25s;
            border-radius:0px 30px;
        }
        nav ul li a:hover{
            background-color:rgba(0,0,0,0.7);
            color:#fff;
        }
        nav ul li ul {
            position:absolute;
            width:200px;
            left:200px;
            top:0;
            border-radius:5px;
            background-color:rgba(0,0,0,0.2);
            box-shadow:2px 2px 10px rgba(0,0,0,0.1)
        }
        nav ul .dropdown ul{
            opacity: 0;
        }
        nav ul .dropdown:hover ul{
            transition: 00.25s;
            opacity: 1;
        }

        input[type='checkbox']{
           opacity: 0;
           position: absolute;
        }
        label{
            margin-top: 5px;
            margin-left:10px;
            position: absolute;
            display: block;
            width:40px;
            height:40px;
            background-image:url(img/first_icon.svg)
        }
        input[type = 'checkbox']:checked ~ label{
           background-image: url(img/end_icon.svg);
        }
        input[type = 'checkbox']:checked ~ .navbar{
            display: block;
        }
    </style>
</head>
<body>
    <div class="contain">
            <input type="checkbox" id="checkbox">
            <label id="check" for="checkbox"></label>
            <div class="" style="width: 100px; height:100px"></div>
        <nav class="navbar">
            <ul>
               <li class="dropdown"> <a href="#">setting</a>
                <ul> 
                    <li><a href="/setuser">Edit client data</a></li>
                    <li><a href="/setgame">Edit game data</a></li>
                </ul>
            </li>
               <li><a href="#">none</a></li>
               <li><a href="#">none</a></li>
               <li><a href="#">none</a></li>
            </ul>
        </nav>
    </div>
</body>
</html>